#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
                           http://www.springframework.org/schema/tx
                           http://www.springframework.org/schema/tx/spring-tx-4.2.xsd">

    <!-- DATA SOURCE CONFIGURATION -->
    <bean name="dataSourceSingular" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${symbol_dollar}{database.driverClassName}"/>
        <property name="url" value="${symbol_dollar}{database.url}"/>
        <property name="username" value="${symbol_dollar}{database.username}"/>
        <property name="password" value="${symbol_dollar}{database.password}"/>
    </bean>

    <!-- HIBERNATE CONFIGURATION -->
    <bean id="sessionFactory" class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSourceSingular"/>
        <property name="packagesToScan" value="org.opensingular"/>
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">com.opensingular.bam.persistence.hibernate.SingularOracle10gDialect</prop>
                <prop key="hibernate.connection.isolation">2</prop>
                <prop key="hibernate.jdbc.batch_size">30</prop>
                <prop key="hibernate.show_sql">${symbol_dollar}{database.show_sql}</prop>
                <prop key="hibernate.format_sql">${symbol_dollar}{database.show_sql}</prop>
                <prop key="hibernate.cache.region.factory_class">org.hibernate.cache.ehcache.SingletonEhCacheRegionFactory</prop>
                <prop key="net.sf.ehcache.configurationResourceName">/ehcache.xml</prop>
                <prop key="hibernate.cache.use_query_cache">true</prop>
                <prop key="hibernate.cache.use_second_level_cache">true</prop>
            </props>
        </property>
    </bean>

    <!-- HIBERNATE TRANSACTION CONFIGURATION -->
    <tx:annotation-driven proxy-target-class="true"/>
    <bean id="transactionManager" class="org.springframework.orm.hibernate4.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory"/>
        <property name="dataSource" ref="dataSourceSingular"/>
    </bean>

</beans>
